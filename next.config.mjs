/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/cognitive-biases",
  output: "export", // <=== enables static exports
  reactStrictMode: true,
};

export default nextConfig;
